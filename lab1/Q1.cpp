// WAP to compute area of triangle

#include<iostream>
using namespace std;

int main()
{
    int length,height;
    cout<<"Enter the length of triangle:";
    cin>>length;
    cout<<"Enter the height of triangle:";
    cin>>height;
    cout<<"The area of triangle is "<<(0.5*length*height);
}